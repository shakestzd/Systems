---
title: "The $13 Trillion AI Bet"
---

```js
// ── Data (Observable FileAttachment — stays here, it's Framework-specific) ─
const stats    = await FileAttachment("data/stats.json").json();
const capex    = await FileAttachment("data/capex.json").json();
const mktcap   = await FileAttachment("data/mktcap.json").json();
const cloudRev = await FileAttachment("data/cloud_rev.json").json();

// byTicker is used in the prose below for inline stat interpolation
const byTicker = Object.fromEntries(mktcap.map(r => [r.ticker, r]));
```

<div class="byline">Thandolwethu Zwelakhe Dlamini · TZD Labs Systems Research</div>

# The $13 Trillion AI Bet

The six biggest AI infrastructure builders spent $${stats.capex_2025.toFixed(0)}B on capital expenditure in 2025. Three of them report the revenue stream that spending is supposed to justify: AWS, Google Cloud, and Azure combined earned $${stats.cloud_rev_2025.toFixed(0)}B in cloud services. The other three don't break it out. None of them reports what AI specifically earns. Seven US tech companies added $${stats.mkt_gain_t.toFixed(1)} trillion in market value since January 2023.

<div id="scroll-section" class="scroll-hero">
  <div id="sticky-valuation" class="scroll-hero-chart">
    <!-- interactive bar chart mounts here -->
  </div>
  <div class="scroll-steps">

<div class="scroll-step is-active" data-company="NVDA">

<strong>Nvidia added +$${(byTicker.NVDA?.gain_t ?? 0).toFixed(1)}T</strong> — the largest gain of the seven, and larger than Germany's GDP. Nvidia doesn't own a data center or run a cloud; it makes the chips the others buy. Its valuation rests on demand for its chips continuing to grow. That requires every major cloud company to keep spending at its current pace.

</div>

<div class="scroll-step" data-company="GOOGL">

<strong>Alphabet added +$${(byTicker.GOOGL?.gain_t ?? 0).toFixed(1)}T</strong> while already spending more on infrastructure than Google Cloud earns. Its AI bet is partly funded from search advertising — $${stats.google_search_ad_rev_qtr_bn}B per quarter from the same engine that AI could eventually disrupt.

</div>

<div class="scroll-step" data-company="AAPL">

<strong>Apple added +$${(byTicker.AAPL?.gain_t ?? 0).toFixed(1)}T</strong> with almost none of the infrastructure spend. Apple spent $${stats.apple_capex_2024_bn}B on infrastructure in 2024 and routes complex AI tasks to OpenAI and Google rather than building its own chip clusters. It has no long-term compute lease obligations with AI hardware vendors.

</div>

<div class="scroll-step" data-company="META">

<strong>Meta added +$${(byTicker.META?.gain_t ?? 0).toFixed(1)}T</strong> after laying off roughly a quarter of its workforce in 2022–2023. It is now one of the most aggressive spenders, planning to spend $${stats.meta_2026g}B in 2026 — about ${Math.round((stats.meta_2026g / stats.meta_2025 - 1) * 100)}% more than it spent in 2025.

</div>

<div class="scroll-step" data-company="AMZN">

<strong>Amazon added +$${(byTicker.AMZN?.gain_t ?? 0).toFixed(1)}T</strong> and crossed the line in 2025 where infrastructure spending now exceeds what Amazon Web Services earns — $${stats.amzn_2025.toFixed(0)}B in spending against $${stats.aws_rev_2025.toFixed(0)}B in annual cloud revenue.

</div>

<div class="scroll-step" data-company="MSFT">

<strong>Microsoft added +$${(byTicker.MSFT?.gain_t ?? 0).toFixed(1)}T</strong>. What doesn't show in its reported spending: in three months (Sep–Nov 2025) it signed over $${stats.msft_neocloud_total_bn}B in leases with third-party data center operators — kept off the books as operating costs rather than direct investment.

</div>

<div class="scroll-step" data-company="TSLA">

<strong>Tesla added +$${(byTicker.TSLA?.gain_t ?? 0).toFixed(1)}T</strong> on AI expectations rather than the car business. Investors pay about 160× expected earnings per share, versus 6× for a typical auto company. It spent $5B on AI infrastructure in 2024 and plans $20B+ in 2026.

</div>

<div class="scroll-step" data-company="all">

<strong>Combined, the seven added $${mktcap.reduce((s, r) => s + r.gain_t, 0).toFixed(1)}T</strong> against $${stats.capex_2025.toFixed(0)}B in 2025 infrastructure spending.

</div>

  </div><!-- .scroll-steps -->
</div><!-- .scroll-hero -->

```js
import { mountValuation } from "./js/charts/valuation.js";
const teardown = mountValuation(document.getElementById("scroll-section"), { mktcap, stats });
invalidation.then(teardown);
```

---

## How did we get here

The previous cloud infrastructure cycle (2015–2021) roughly doubled spending every six years. This one doubled in two.

```js
import { createCapexHistory } from "./js/charts/capex-history.js";
import { mountScrollChart } from "./js/animate.js";

const capexHistoryChart = createCapexHistory(capex, stats);
// Callout anchored in the upper-center — above the pre-2022 line (which stays
// below $150B), near the 2017–2019 range on the x-axis. As the line rises
// post-2022, the callout stays above the historical data it's narrating.
display(mountScrollChart(capexHistoryChart.node, capexHistoryChart.update, [
  { prose: "Seven years of steady growth — doubling roughly every cycle.", position: { top: "18%", left: "20%", maxWidth: "22ch" } },
  { prose: "2022: the inflection. ChatGPT launches. A new spending race begins.", position: { top: "18%", left: "20%", maxWidth: "22ch" } },
  { prose: "Two years to double what took six. The fastest acceleration in the industry's history.", position: { top: "18%", left: "20%", maxWidth: "22ch" } },
  { prose: `$${stats.capex_4co_2025.toFixed(0)}B in 2025 — ${stats.capex_4co_multiple}× the 2019 baseline.`, position: { top: "18%", left: "20%", maxWidth: "22ch" } },
]));
```

## Who is building — and how much

Amazon has led total spending since 2022. The hatched 2026 column shows what each company says it plans to spend — though those plans have shifted by ±${stats.guidance_band_pct}% in a single year before.

```js
import { createStackedCapex } from "./js/charts/stacked-capex.js";

const stackedCapexChart = createStackedCapex(capex, stats);
// Callout anchored upper-right — above Oracle and Nvidia's shorter bars, clear of the
// stacked data for Amazon, Alphabet, and Microsoft that fills the left side of the chart.
display(mountScrollChart(stackedCapexChart.node, stackedCapexChart.update, [
  { prose: "Amazon, Alphabet, Microsoft, Meta, Oracle, and Nvidia — what each spent, year by year since 2022.", position: { top: "14%", right: "4%", maxWidth: "22ch" } },
  { prose: "Amazon has spent more than everyone else since 2022.", position: { top: "14%", right: "4%", maxWidth: "22ch" } },
  { prose: `Meta barely participated in 2022. They now plan to spend $${stats.meta_2026g}B in 2026.`, position: { top: "14%", right: "4%", maxWidth: "22ch" } },
  { prose: `Together, the six companies plan to spend $${stats.guidance_2026_point.toFixed(0)}B in 2026. Those plans have moved by ±${stats.guidance_band_pct}% in a single year before.`, position: { top: "14%", right: "4%", maxWidth: "22ch" } },
]));
```

Between September and November 2025, Microsoft signed over $${stats.msft_neocloud_total_bn}B in leases with neocloud companies (firms that build and lease AI compute capacity rather than running their own cloud services): Nebius ($${stats.msft_nebius_deal_bn}B), Nscale ($${stats.msft_nscale_deal_bn}B), Iren ($${stats.msft_iren_deal_bn}B), plus Lambda. Meta financed $${stats.meta_beignet_financing_bn}B of Louisiana data center construction through a separate shell company called Beignet Investor LLC, with Blue Owl Capital providing 80% of the funding. Together, just those two commitments add $${(stats.msft_neocloud_total_bn + stats.meta_beignet_financing_bn).toFixed(0)}B in infrastructure obligations that don't appear in reported capex totals.

```js
import { createOffBalance } from "./js/charts/off-balance.js";

const offBalanceChart = createOffBalance(stats);
// Callout in the center gap between the two bars — bar spacing widened (paddingInner 0.5)
// so the gap between Microsoft and Meta is clear from ~39% to ~64% of chart width.
// Callout at left:40% sits in that gap, above the bars which start at ~23% from top.
display(mountScrollChart(offBalanceChart.node, offBalanceChart.update, [
  { prose: `The six companies' 2025 annual reports show a combined $${stats.capex_2025.toFixed(0)}B in direct infrastructure spending.`, position: { top: "14%", left: "40%", maxWidth: "18ch" } },
  { prose: "Long-term leases and off-balance-sheet financing vehicles add infrastructure commitments not captured in that total.", position: { top: "14%", left: "40%", maxWidth: "18ch" } },
  { prose: `Microsoft: $${stats.msft_2025.toFixed(0)}B reported. Neocloud leases add $${stats.msft_neocloud_total_bn}B not in that figure.`, position: { top: "14%", left: "40%", maxWidth: "18ch" } },
]));
```

*A note on accounting: US rules that took effect in 2019 require long-term leases to appear on a company's balance sheet — but the payments still count as operating costs, not capital investment, so they drop out of the infrastructure spending totals that analysts and media typically report.*

## Revenue vs Capital

Amazon, Alphabet, and Microsoft each report cloud revenue as a separate segment — AWS, Google Cloud, and Intelligent Cloud. They are the only hyperscalers that do, which makes a direct comparison possible: what each company spent against what came back.

```js
import { createRevenueRatio } from "./js/charts/revenue-ratio.js";

const revenueRatioChart = createRevenueRatio(capex, cloudRev);
// Annotations live in the bottom-margin strip inside the SVG (stepAnnot pattern) —
// no CSS overlay needed; callout: "none" suppresses the floating div entirely.
display(mountScrollChart(revenueRatioChart.node, revenueRatioChart.update, [
  {}, {}, {}, {}
], { callout: "none" }));
```

Amazon's AWS is the largest of the three cloud segments. Microsoft's Intelligent Cloud is the only one where quarterly capex still hasn't crossed above cloud revenue — but that gap fell from $${stats.msft_ic_gap_q3_2025.toFixed(1)}B to $${stats.msft_ic_gap_q4_2025.toFixed(1)}B in a single quarter.

```js
import { createRevenueGap } from "./js/charts/revenue-gap.js";

const revenueGapChart = createRevenueGap(capex, cloudRev);
// Callout anchored upper-left — in early 2023 all lines start near the bottom-left,
// leaving the upper-left of the chart clear before the divergence becomes visible.
display(mountScrollChart(revenueGapChart.node, revenueGapChart.update, [
  { prose: "In early 2023, infrastructure spending and cloud revenue were roughly in sync.", position: { top: "18%", left: "8%", maxWidth: "24ch" } },
  { prose: "By 2024, infrastructure spending was growing faster than the cloud revenue it was built to serve.", position: { top: "18%", left: "8%", maxWidth: "24ch" } },
  { prose: "For Alphabet and Amazon, infrastructure spending now exceeds what their cloud businesses earn.", position: { top: "18%", left: "8%", maxWidth: "24ch" } },
]));
```

## One Spending Line, Six Demand Assumptions

The companies don't agree on what the spending is for. In September 2025, the New York Times (Metz & Weise) mapped at least six distinct demand theses being pursued simultaneously. The categories matter because they have different revenue timelines — some measured in quarters, others with no disclosed timeline at all.

```js
import { createDemandThesis } from "./js/charts/demand-thesis.js";
display(createDemandThesis(stats));
```

Enterprise AI services (OpenAI API integrations, GitHub Copilot) are billing now. Autonomous agent bets have no revenue timeline disclosed in any company filing. All six theses draw from the same capex pool.

## Spending Forecasts Have Been Wrong Before — in Both Directions

Companies have collectively forecast $${stats.guidance_2026_point.toFixed(0)}B in infrastructure spending for 2026. Those forecasts have historically been off by ±${stats.guidance_band_pct}%. Meta slashed its plans when its advertising business contracted in 2022–2023. Microsoft then raised its plans when cloud demand grew faster than expected. Both moved by roughly a third in a single year.

```js
import { createGuidance } from "./js/charts/guidance.js";

const guidanceChart = createGuidance(stats);
// Callout anchored upper-right — Meta (rightmost) typically has the shortest 2025 actual bar,
// leaving clear space in the upper-right corner above it before the error bands appear at step 2.
display(mountScrollChart(guidanceChart.node, guidanceChart.update, [
  { prose: `The six companies spent a combined $${stats.capex_2025.toFixed(0)}B on infrastructure in 2025.`, position: { top: "12%", right: "4%", maxWidth: "22ch" } },
  { prose: `Their combined 2026 guidance: $${stats.guidance_2026_point.toFixed(0)}B.`, position: { top: "12%", right: "4%", maxWidth: "22ch" } },
  { prose: `Spending plans have shifted by ±${stats.guidance_band_pct}% in a single year before — Meta cut by roughly a third in 2022; Microsoft raised by roughly a third in FY2025.`, position: { top: "12%", right: "4%", maxWidth: "22ch" } },
]));
```

## What Happens in 2026

```js
import { createScenarios } from "./js/charts/scenarios.js";

const scenariosChart = createScenarios(stats);
// The SVG handles its own step annotations using D3-scale positions (see scenarios.js).
// No CSS overlay needed — positions are computed from the same x/y scales as the data,
// so they're perfectly responsive at every viewport width.
display(mountScrollChart(scenariosChart.node, scenariosChart.update, [
  {}, {}, {}, {}, {}
], { callout: "none" }));
```

The bear case is the counterintuitive one: if cloud demand weakens, both capex and revenue fall — but the gap stays wide, because the same weakness that cuts spending also cuts the revenue it was meant to generate. There is no path where weak demand closes the gap on its own.

Nvidia's market cap fell $${stats.nvda_deepseek_loss_bn.toFixed(0)} billion on January 27, 2025 — the largest single-day market cap loss in US stock market history. The trigger was DeepSeek's release of a reasoning model that matched GPT-4-class benchmarks. DeepSeek's base model was trained for roughly $${stats.deepseek_v3_train_cost_m.toFixed(0)} million in compute; Sam Altman said GPT-4 cost "more than $100 million." The $${stats.deepseek_v3_train_cost_m.toFixed(0)} million covers only the final training run and excludes prior experiments and hardware costs. On inference pricing, the gap is real: DeepSeek's API charges roughly 4–5 times less per token than GPT-4o. If inference efficiency keeps improving, the chip volume required per unit of AI output falls with it. For Amazon and Alphabet, infrastructure spending already exceeds what their cloud businesses earn. Sustaining current valuations requires that gap to close.

The financial commitments that created those valuations convert to physical assets on a different timeline. A data center under construction, a signed power purchase agreement, a transformer in the interconnection queue — these don't reprice in a day. What's already sunk and what's still convertible is what the next article traces.

---

*This is the first of three DD-001 articles. Next: [The Conversion Gap](/Systems/dd001-conversion) traces how slowly capital converts to operating infrastructure. Then: [Who Holds the Downside](/Systems/dd001-risk) follows the risk to its final bearers.*

## Sources & Methodology

**Market capitalizations.** Yahoo Finance daily closing prices. Gain calculated as difference between 2026-02-14 and 2023-01-03 market caps for each of the seven companies.

**Capital expenditure (2015–2025).** SEC 10-K and 10-Q filings for Amazon, Alphabet, Microsoft, Meta, Oracle, and Nvidia, retrieved via yfinance. Annual figures are fiscal-year sums of quarterly reported capex. 2015–2021 figures from company annual reports where quarterly data was unavailable.

**2026 guidance.** Q4 2025 earnings calls and investor presentations. Amazon ($${stats.amzn_2026g}B), Alphabet ($${stats.googl_2026g}B), Microsoft ($${stats.msft_2026g}B), Meta ($${stats.meta_2026g}B). Uncertainty band (±${stats.guidance_band_pct}%) based on the largest single-year guidance revision in the 2020–2025 period (Meta 2022–2023 cut; Microsoft FY2025 raise).

**Cloud segment revenue.** AWS, Google Cloud, and Intelligent Cloud quarterly revenue from SEC 10-K/10-Q filings. These are the only three hyperscalers that report cloud as a separate segment.

**Off-balance-sheet commitments.** Microsoft neocloud leases: Nebius ($${stats.msft_nebius_deal_bn}B), Nscale ($${stats.msft_nscale_deal_bn}B), Iren ($${stats.msft_iren_deal_bn}B), plus Lambda — reported in company announcements and *New York Times*, Dec 15, 2025 (Weise & Tan). Meta Beignet Investor LLC: $${stats.meta_beignet_financing_bn}B Louisiana data center financing via Blue Owl Capital.

**Revenue demand theses.** Classification based on *New York Times*, Sep 2025 (Metz & Weise), cross-referenced with company earnings calls and product announcements.

**DeepSeek training cost.** DeepSeek-V3 technical report (arXiv:2412.19437, December 2024) discloses 2,788,000 H800 GPU hours for the final pre-training run; at approximately $2/GPU-hour market rate, this implies ~$5.6M in compute. Covers final training run only — excludes prior experiments and hardware acquisition. GPT-4 training cost: Sam Altman public statement, April 2023 (reported by Business Insider and Fortune). Inference pricing: DeepSeek official API (platform.deepseek.com) vs OpenAI API (openai.com/api/pricing).

**Nvidia market cap loss.** $${stats.nvda_deepseek_loss_bn.toFixed(0)} billion on January 27, 2025 — the largest single-day market cap loss in US stock market history. Sources: Reuters, Yahoo Finance, and CNBC, all reporting January 27, 2025.

**Scenario projections.** TZD Labs analysis. Cloud revenue range ($${stats.cloud_rev_2026_low}–$${stats.cloud_rev_2026_high}B) based on trailing growth rates applied to 2025 actuals. Bull case assumes acceleration; base case assumes current trajectory; bear case applies the ±${stats.guidance_band_pct}% guidance band to both capex and revenue.

*All data from public, freely accessible sources. No proprietary data or paywalled databases. Analysis: TZD Labs, February 2026.*
